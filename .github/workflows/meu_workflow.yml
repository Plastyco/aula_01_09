name: meu workflow

on:

  push:
    branches: 
      - master
      - '**.html'

  
  workflow_dispatch:

jobs:
  do_job:

    runs-on: ubuntu-latest

    steps:
      - name: Boas-vindas
        run: echo "salve salve"
      - name: Mostra o diretório de trabalho
        run: pwd
      - name: Lista os arquivos no diretório home
        run: ls ~ -a
      - name: Mostra o repo
        run: echo "Este job ta no repositório ${{ github.repository }}"
